# CVAE AFHQ 向け潜在空間クラスタリングを改善

1.  ConditionalVAE を修正し、クラス識別可能な潜在表現を促進しました。

    - `models/cvae.py`のデコーダー入力から one-hot 処理を削除し、エンコーダーがクラス情報を潜在空間に埋め込むように強制しました。
    - `configs/afhq_experiments/cvae_afhq_clustering.yaml`を作成し、学習用に`kld_weight`を調整しました。

2.  ロジスティック回帰（80.00%の精度）と LDA プロットにより、潜在空間が「猫」と「犬」のクラスを効果的に分離することを確認しました。