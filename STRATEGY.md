# `MyTaste` VAE プロジェクト戦略

## 主目標

個人の好み（「アリ 'kept'」/「ナシ 'discarded'」）に基づいて二次フィルタリングされた特製のUTK-FACEデータセットで、変分オートエンコーダ（VAE）の学習と分析を行う。モデルが潜在空間において、この主観的な美的特徴を学習し表現することを目指す。

## プロジェクトロードマップ

1.  **データセットの統合 (Dataset Integration):**
    *   `dataset.py`を修正し、特製のフィルタリング済みUTK-FACEデータセットを正しく読み込めるようにする。これには、ディレクトリ構造と好みベースのラベル（'kept'/'discarded'）の処理も含まれる。

2.  **設定ファイルの作成 (Configuration):**
    *   この実験専用の設定ファイル（例: `configs/mytaste_cvae.yaml`）を作成する。
    *   モデルのアーキテクチャ（Conditional VAEが有力候補）、ハイパーパラメータ（バッチサイズ、学習率など）、およびデータセットへのパスを定義する。

3.  **フェーズ1: 性別クラスタリングの検証 (Phase 1: Gender Clustering Verification):**
    *   CVAEモデルを**性別ラベル**（male/female）で条件付けして学習させます。
    *   再構築画像や潜在空間の分析を通じて、モデルが条件付きラベルから正常に学習し、パイプラインが正しく機能していることを検証します。

4.  **フェーズ2: 好みクラスタリングの実施 (Phase 2: Taste Clustering Implementation):**
    *   パイプラインが検証された後、データセットローダーを修正し、**好みラベル**（kept/discarded）を使用するように変更します。
    *   好みラベルで条件付けされたCVAEモデルを学習させます。

5.  **分析と可視化 (Analysis and Visualization):**
    *   好みベースのモデルの結果を分析し、主観的な美的特徴を学習できたかどうかを確認します。
    *   **画像生成:** 学習済みモデルから新しい顔画像を生成し、結果を視覚的に評価する。
    *   **潜在空間の探索:** 潜在空間を探索（Latent Space Traversal）し、潜在変数を操作することが生成される顔の特徴にどのように影響するかを観察する。
    *   **特徴の可視化:** 次元削減技術（例: t-SNE, UMAP）を使用して潜在空間を可視化し、モデルが「アリ」と「ナシ」のラベルに基づいてデータをどのようにクラスタリングしたかを分析する。
